package com.wiom.csp.ui.theme

import androidx.compose.ui.graphics.Color

/** Dark theme colors (default) -- ported from globals.css :root */
object DarkColors {
    // Brand
    val brandPrimary = Color(0xFFD9008D)
    val brandLight = Color(0xFFFFB2E4)
    val brandLighter = Color(0xFFFFCCED)
    val brandLightest = Color(0xFFFFE5F6)
    val brandSecondary = Color(0xFF443152)

    // Gold Gradient
    val goldStart = Color(0xFFD5AF4A)
    val goldMid = Color(0xFFF2E983)
    val goldEnd = Color(0xFFD5B14B)

    // Backgrounds
    val bgPrimary = Color(0xFF161021)
    val bgSecondary = Color(0xFF352D42)
    val bgCard = Color(0xFF443152)
    val bgCardHover = Color(0xFF665E75)

    // Text
    val textPrimary = Color(0xFFFAF9FC)
    val textSecondary = Color(0xFFA7A1B2)
    val textMuted = Color(0xFF665E75)

    // Semantic
    val positive = Color(0xFF008043)
    val positiveLight = Color(0xFFA5E5C6)
    val positiveBg = Color(0xFFE1FAED)
    val negative = Color(0xFFE01E00)
    val negativeLight = Color(0xFFFFBDB3)
    val negativeBg = Color(0xFFFFE9E5)
    val warning = Color(0xFFFF8000)
    val warningDark = Color(0xFFB85C00)
    val warningLight = Color(0xFFFFAA00)
    val warningBg = Color(0xFFFFE6CC)

    // Borders
    val borderSubtle = Color(0xFF352D42)
    val cardBorder = Color(0xFF2A2D3A)

    // Chip
    val chipBg = Color(0xFF352D42)

    // Money
    val money = Color(0xFF2196F3)
    val moneyLight = Color(0xFF90CAF9)
    val moneyBg = Color(0x1F2196F3)

    // Task accents
    val accentRestore = Color(0xFF6D17CE)
    val accentGold = Color(0xFFD5AF4A)

    // Strip
    val stripBg = Color(0xFF161021)

    // Subtle backgrounds
    val positiveSubtle = Color(0x1A008043)
    val negativeSubtle = Color(0x14E01E00)
    val warningSubtle = Color(0x1AFF8000)
    val brandSubtle = Color(0x14D9008D)
    val moneySubtle = Color(0x142196F3)
    val restoreSubtle = Color(0x266D17CE)
    val goldSubtle = Color(0x26D5AF4A)

    // Card gradient
    val cardGradientStart = Color(0xFF443152)
    val cardGradientEnd = Color(0xFF665E75)

    // Overlay
    val overlayBg = Color(0x99000000)
}

/** State-color-check theme (warm off-white) -- ported from [data-theme="state-color-check"] */
object LightColors {
    // Brand
    val brandPrimary = Color(0xFFC10080)
    val brandLight = Color(0xFFF2A0D0)
    val brandLighter = Color(0xFFFFCCED)
    val brandLightest = Color(0xFFFFE5F6)
    val brandSecondary = Color(0xFFF0EDE6)

    // Gold Gradient
    val goldStart = Color(0xFFD5AF4A)
    val goldMid = Color(0xFFF2E983)
    val goldEnd = Color(0xFFD5B14B)

    // Backgrounds
    val bgPrimary = Color(0xFFFAF7F2)
    val bgSecondary = Color(0xFFF0EDE6)
    val bgCard = Color(0xFFFFFFFF)
    val bgCardHover = Color(0xFFF5F3EE)

    // Text
    val textPrimary = Color(0xFF1C1B1F)
    val textSecondary = Color(0xFF5E5C66)
    val textMuted = Color(0xFF9D9BA3)

    // Semantic
    val positive = Color(0xFF2B7A4B)
    val positiveLight = Color(0xFF8CCDA5)
    val positiveBg = Color(0xFFE1FAED)
    val negative = Color(0xFFC4453A)
    val negativeLight = Color(0xFFE8A9A4)
    val negativeBg = Color(0xFFFFE9E5)
    val warning = Color(0xFFB87D00)
    val warningDark = Color(0xFF8A5E00)
    val warningLight = Color(0xFFD4A73D)
    val warningBg = Color(0xFFFFE6CC)

    // Borders
    val borderSubtle = Color(0xFFE4E1DA)
    val cardBorder = Color(0xFFE4E1DA)

    // Chip
    val chipBg = Color(0xFFF0EDE6)

    // Money
    val money = Color(0xFF1976D2)
    val moneyLight = Color(0xFF64A8E8)
    val moneyBg = Color(0x141976D2)

    // Task accents
    val accentRestore = Color(0xFF5B12AC)
    val accentGold = Color(0xFF9E7E2A)

    // Strip
    val stripBg = Color(0xFFEDE9E2)

    // Subtle backgrounds
    val positiveSubtle = Color(0x0F2B7A4B)
    val negativeSubtle = Color(0x0FC4453A)
    val warningSubtle = Color(0x0FB87D00)
    val brandSubtle = Color(0x0FC10080)
    val moneySubtle = Color(0x0F1976D2)
    val restoreSubtle = Color(0x145B12AC)
    val goldSubtle = Color(0x149E7E2A)

    // Card gradient
    val cardGradientStart = Color(0xFFFFFFFF)
    val cardGradientEnd = Color(0xFFF5F3EE)

    // Overlay
    val overlayBg = Color(0x4D000000)
}
